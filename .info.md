# Info Proyek

Catatan internal untuk deployment, perintah umum, dan ringkasan perubahan.

## Hostinger (CloudLinux): PHP 8.3 untuk CLI/Composer/Artisan

Di server Hostinger/CloudLinux, perintah `php` via SSH bisa menunjuk ke PHP CLI default (mis. 8.2) karena memakai selector (`/etc/cl.selector/php-cli`).
Jika ingin menjalankan perintah dengan PHP 8.3, gunakan binary ini:

```bash
/opt/alt/php83/usr/bin/php -v
```

Untuk konsistensi, gunakan path PHP 8.3 untuk semua perintah di server (artisan/composer/cron/queue).

### Composer (Production)

```bash
/opt/alt/php83/usr/bin/php /usr/local/bin/composer install --no-dev --optimize-autoloader
/opt/alt/php83/usr/bin/php artisan -V
/opt/alt/php83/usr/bin/php -v
```

### Artisan (gunakan PHP 8.3)

```bash
/opt/alt/php83/usr/bin/php artisan optimize:clear
/opt/alt/php83/usr/bin/php artisan config:cache
/opt/alt/php83/usr/bin/php artisan route:cache
/opt/alt/php83/usr/bin/php artisan view:cache
```

### Cron/Queue (wajib konsisten)

Pastikan cronjob/scheduler/queue worker memanggil artisan dengan path PHP 8.3 di atas (bukan `php artisan ...`)
agar tidak kembali ke PHP CLI default.

Contoh:

```bash
/opt/alt/php83/usr/bin/php artisan schedule:run
/opt/alt/php83/usr/bin/php artisan queue:work
```

## Lokasi Folder Server (Hostinger)

```bash
cd /home/u380354370/domains/maknafinance.id/public_html
cd /home/u380354370/domains/update.maknakreatif.id/public_html
cd /home/u380354370/domains/wofins.id/public_html
cd /home/u380354370/domains/thewhitefinance.com/public_html
cd /home/u380354370/domains/patrononline.id/public_html
cd /home/u380354370/domains/prasetyowo.com/public_html
```

Google Cloud Console:

- <https://console.cloud.google.com/>

## Artisan (Migrasi & Filament)

Local (dev):

```bash
php artisan migrate
php artisan optimize:clear
php artisan shield:generate --all
```

Server (Hostinger, PHP 8.3):

```bash
/opt/alt/php83/usr/bin/php artisan migrate:status
/opt/alt/php83/usr/bin/php artisan optimize:clear
/opt/alt/php83/usr/bin/php artisan config:cache
/opt/alt/php83/usr/bin/php artisan route:cache
/opt/alt/php83/usr/bin/php artisan view:cache
/opt/alt/php83/usr/bin/php /usr/bin/composer dump-autoload
```

Migrasi spesifik file:

```bash
/opt/alt/php83/usr/bin/php artisan migrate --path=database/migrations/2025_08_30_094406_create_company_logos_table.php
/opt/alt/php83/usr/bin/php artisan migrate --path=database/migrations/2025_09_01_100318_create_blogs_table.php

/opt/alt/php84/usr/bin/php artisan migrate --path=database/migrations/2026_05_25_222615_create_activity_log_table.php
```

## Snippet (Uang / Format)

```php
->prefix('Rp. ')
->mask(RawJs::make('$money($input)'))
->stripCharacters(',')
->dehydrateStateUsing(fn ($state) => (int) preg_replace('/[^\d]/', '', (string) $state))
->placeholder('0')
```

```php
->formatStateUsing(fn ($state): string => $state ? Str::ucfirst($state) : '-')
```

## Git Workflow (Deploy)

```bash
git add .
git commit -m "Update"
git push origin main
```

## Build & Dependencies

Local:

```bash
npm run dev
composer update
composer dump-autoload
```

Server (production):

```bash
/opt/alt/php83/usr/bin/php /usr/local/bin/composer install --no-dev --optimize-autoloader
```

ok

## Clear Cache / Optimize

```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan optimize:clear
php artisan optimize
php artisan filament:optimize

git add .
git commit -m "update"
git push origin main
```

## Cache / Optimize

````bash
cd /home/u380354370/domains/maknafinance.id/public_html
git pull
/opt/alt/php84/usr/bin/php artisan cache:clear
/opt/alt/php84/usr/bin/php artisan config:clear
/opt/alt/php84/usr/bin/php artisan route:clear
/opt/alt/php84/usr/bin/php artisan view:clear
/opt/alt/php84/usr/bin/php artisan optimize:clear
/opt/alt/php84/usr/bin/php artisan optimize
/opt/alt/php84/usr/bin/php artisan filament:optimize

/opt/alt/php84/usr/bin/php artisan migrate:status

php artisan db:seed --class=BlogSeeder

/opt/alt/php84/usr/bin/php /usr/local/bin/composer update

https://maknafinance.id/log-viewer

npm run build
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan optimize:clear
php artisan optimize
php artisan filament:optimize
git add .
git commit -m "Update"
git push origin main

http://127.0.0.1:8000/_laravel-brain

php artisan shield:generate --all

## Debug (PHP Info)

Endpoint internal untuk cek versi PHP web via browser (hanya super-admin):

- `/_phpinfo`

## Hostinger: Paksa Server Sama Dengan Git

Jalankan di terminal Server/Hostinger (reset semua perubahan lokal di server):

```bash
git fetch --all
git reset --hard origin/main
```

```bash
composer2 dump-autoload
```

## Catatan Versi

- File ini pernah dioptimasi pada Filament v4.0.10
- Update: 29 Nov 2025

## Resume Perubahan (30 Des 2025 - 04 Jan 2026)

### Dokumen & PDF

- Pembaruan besar pada template PDF dokumen (`resources/views/documents/pdf.blade.php`) termasuk layout dan konten.
- Penambahan fitur `Confidentiality Warning` pada dokumen (Migration & Form).
- Penambahan fitur `Digital Signature` pada dokumen (Migration & Form).
- Update `DocumentForm.php` dan `DocumentObserver`.

### Kategori Dokumen

- Update format nomor pada `DocumentCategory` (Migration & Seeder).

### Resource & Konfigurasi

- Update `UserResource.php` dan `EmployeeResource.php`.
- Update `config/app.php`.

### History Commit

- 02 Jan 2026: Penambahan `show_confidentiality_warning`
- 01 Jan 2026: Penambahan `use_digital_signature`
- 01 Jan 2026: Update format nomor kategori dokumen
- 01 Jan 2026: Update PDF dokumen (multiple commits)
- 31 Des 2025: Update awal template PDF
````
